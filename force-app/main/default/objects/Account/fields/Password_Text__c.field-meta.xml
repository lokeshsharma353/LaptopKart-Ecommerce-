<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Password_Text__c</fullName>
    <description>Stored unencrypted by explicit design (same decision as the original Login_Credentials__c object this replaces) — not a security best practice, but intentional so admins can view a shopper&apos;s password directly on the record.</description>
    <externalId>false</externalId>
    <label>Password (Plain Text)</label>
    <length>255</length>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
