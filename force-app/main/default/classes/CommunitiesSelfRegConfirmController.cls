/**
 * Backs the CommunitiesSelfRegConfirm Visualforce page (the "check your
 * email to finish registering" confirmation screen shown after
 * CommunitiesSelfRegController.registerUser() creates a user with no
 * password). Has no logic of its own — the page just displays static text.
 */
public with sharing class CommunitiesSelfRegConfirmController {

    public CommunitiesSelfRegConfirmController() {}
}